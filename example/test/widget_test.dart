// Example app smoke test — package logic is covered in the root test/ suite.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('example package loads', () {
    expect(true, isTrue);
  });
}
