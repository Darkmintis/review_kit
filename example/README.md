# ReviewKit Example

Interactive demo for [`in_app_review_kit`](https://pub.dev/packages/in_app_review_kit).

```bash
cd example
flutter pub get
flutter run
```

Use the buttons to track events, check eligibility, request a review, and open
the built-in `ReviewDebugScreen` via the bug icon.
